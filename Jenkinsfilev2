pipeline{
  agent any
    stages{
      stage('Build'){
        steps{
         echo"Installing dependencies..."
         sh 'pip install -r requirements.txt'
        }
      }
      stage('Test'){
        steps{
          echo"Running tests"
          sh 'python3 test.py'
        }
      }
    }
}
